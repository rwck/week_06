<p id="notice"><%= notice %></p>

<h1>Listing Projects</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>User</th>
      <th>
        User name
      </th>
      <th>
        Blob
      </th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @projects.each do |project| %>

    <tr>
      <td><%= project.name %></td>
      <td><%= project.user.id %></td>
      <td>
        <%= @users.find(project.user_id).firstname %>
        <%= @users.find(project.user_id).lastname %>
      </td>
      <td>
        <%= link_to 'Add issue', new_project_issue_path(project) %>
      </td>
      <% if false %>
      new_issue_path(@user, param1: 'hello'), method: :post %> link_to "Profile", profile_path(@new_issue.id, param1: 'value1', param2: 'value2'), method: :post

      <% end %>

      <td><%= link_to 'Show', project %></td>
      <td><%= link_to 'Edit', edit_project_path(project) %></td>

      <td>
        <% if !project %>
        <%= link_to 'Destroy', project, method: :delete, data: { confirm: 'Are you sure?' } %>
        <% end %>
      </td>
    </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Project', new_project_path %>